const URL = 'https://foutalib-com.onrender.com/';
const productsURL = `${URL}product`;
const registerURL =`${URL}register-user`;
const loginURL = `${URL}login`;
const addProductURL = `${URL}add-product`;
const categoryURL = `${URL}category`;
const ImageURL = `${URL}images/`;
const mainImageURI = `${URL}image/`;
const ImagePathURI =`${URL}image-path`
const prodcutReviewURL = `${URL}review/`;
const addCartURL = `${URL}cart-add`;
const CartURL = `${URL}cart/`;
const deleteCart = `${URL}/cart-delete/`;
const addOrderURL = `${URL}order-add`;
const orderURL = `${URL}order/`;
const searchURL = `${URL}search`;
const vendorProductURL = `${URL}vendorsproduct/`;
const addPaymentURL = `${URL}add-payment`;
const updateCart = `${URL}cart-update/`; 
const paymentURL = `${URL}payment/`
const avatarURL = `${URL}avatar/`
const addAvatarURL = `${URL}avatar-upload/`
const vendorUrl = `${URL}vendor/`
const addFavoriteUrl = `${URL}add-favorite`
const favoriteUrl = `${URL}favorite/`
const favoriteDelete = `${URL}delete-favorite/`
const vendorRegisterURL = `${URL}vendor-register`
const categoryProduct = `${URL}category-products/`
const singleProductURL = `${URL}single-product/`
const addImageURL = `${URL}upload-image/`
const updateProductURL = `${URL}update-product/`
const allUserURL = `${URL}all-user`
const addUser = `${URL}add-user`
const addCategory = `${URL}add-category`
const categoryImage =`${URL}cat-image/`
const InvoiceURL =`${URL}invoice/`
const adminLoginURL=`${URL}admin-login`
const adminRegisterURL=`${URL}admin-register`
const deleteProductURL =`${URL}delete-product/`




export{
    productsURL,
    registerURL,
    loginURL,
    addProductURL,
    categoryURL,
    ImageURL,
    prodcutReviewURL,
    addCartURL,
    CartURL,
    addOrderURL,
    orderURL,
    searchURL,
    vendorProductURL,
    addPaymentURL,
    deleteCart,
    updateCart,
    paymentURL,
    avatarURL,
    addAvatarURL,
    vendorUrl,
    addFavoriteUrl,
    favoriteUrl, 
    favoriteDelete,
    vendorRegisterURL,
    categoryProduct,
    singleProductURL,
    addImageURL,
    mainImageURI,
    ImagePathURI,
    allUserURL,
    addUser,
    addCategory,
    categoryImage,
    InvoiceURL,
    adminLoginURL,
    adminRegisterURL,
    updateProductURL,
    deleteProductURL


}  